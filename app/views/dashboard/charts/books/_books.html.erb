<section class="flex w-full flex-col items-center border">
        <h3 class="text-white font-bold text-lg">Books Sales Report </h3>

        <div  class="flex flex-col" id="books_charts">
                <% Book.all.each do |book| %>
                       
                        <%= render 'dashboard/charts/books/chart', book: book %>
                <% end %>
        </div>
</section>